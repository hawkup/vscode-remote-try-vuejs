{
    "name": "VSCode Remote Try VueJS",
    "dockerFile": "Dockerfile",

    "settings": {
        "terminal.integrated.shell.linux": "/bin/bash"
    },

    "extensions": [
        "dbaeumer.vscode-eslint"
    ],

    "appPort": 8080,

    "runArgs": ["-p", "hostIP:hostPort:8080"],

    "postCreateCommand": "npm install"
}